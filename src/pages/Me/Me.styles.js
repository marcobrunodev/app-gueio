import styled from 'styled-components'

const Me = styled.main`
  padding-top: var(--height-header-me);
  display: flex;
  flex-direction: column;
  align-items: space-between;
  height: 100vh;
`

export default {
  Me
}
